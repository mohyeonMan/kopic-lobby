spring:
  application:
    name: kopic-lobby
  data:
    redis:
      host: localhost
      port: 6379

kopic:
  node-id: lobby-local
  lobby:
    endpoint: /ws
    max-text-message-size: 8192
  redis:
    room-owner-key-prefix: "room:owner:"
    engine-route-key-prefix: "engine:route:"
